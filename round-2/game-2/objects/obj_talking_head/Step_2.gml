/// @description rcorder
if(wasLeftPressed) levelmanager.move_recorder[array_length_1d(levelmanager.move_recorder)] = wasLeftPressed;
if(wasRightPressed) levelmanager.move_recorder[array_length_1d(levelmanager.move_recorder)] = wasRightPressed;
if(wasDownPressed) levelmanager.move_recorder[array_length_1d(levelmanager.move_recorder)] = wasDownPressed;
//if(nokeypressed) levelmanager.move_recorder[array_length_1d(levelmanager.move_recorder)-1] = nokeypressed;

import { State, StateMachine } from '../../states/state-machine';

export class BlueState implements State {
    stateMachine: StateMachine;

    enter(...args) {
    }

    execute(...args) {
    }
}
